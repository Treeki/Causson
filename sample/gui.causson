type Wind = record {
	gui:Window wnd
	gui:Button butt
}

def main() -> void {
	gui:init()
	let wind = Wind:build(gui:Window:new(), gui:Button:new())
	wind.butt.label = "Bork!"
	wind.wnd.add_child(wind.butt)
	wind.wnd.show()
	gui:run()
}

